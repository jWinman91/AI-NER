AnonymizeAuthor:
  Context: >
    Replace the author name at every instance in the email below by Max Mustermann.
    Output the entire email with the replaced name.
AnonymizeRecipent:
  Context: >
    Replace the recipient at every instance in the email below by John Doe.
    Output the entire email with the replaced name.